apiVersion: v1
kind: Service
metadata:
  name: devops-cicd-app-service
  namespace: devops-demo
  labels:
    app: devops-cicd-app
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: devops-cicd-app
